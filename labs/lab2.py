def task5():

    h = float(input('введите высоту в метрах'))
    #v = float(input('введите начальную скорость'))

    t = (2*h / 9.8)**0.5

    print('время падения =', t, 'с')


def task6():
    
    k = float(input('сторона треугольника = '))
    alpha = float(input('угол альфа = '))
    beta = float(input('угол бета = '))
    
   
